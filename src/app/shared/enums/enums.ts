export enum Path {
    SignIn = "sign-in",
    SignUp = "sign-up",
    Profile = "profile",
    Dashboard = "dashboard",
    UsersList = "users-list",
    ServicesList = "services-list-admin",
    Home = "home",
    Requested = "requested",
    forgetPassword = "forget-password"
}

export enum UserType {
    vendor = 'vendor',
    traveller = 'traveller',
    admin = "admin"
}

export enum VehicleType {
    bus = 'bus',
    car = 'car',
    train = "train"
}

export enum StorageKeys {
    User="User",
    Admin="Admin"
}

export enum Collection{
    Users = "users",
    Services = "services",
}

export enum ViewMode{
    view = "view",
    edit = "edit",
}

export enum TravelType{
    Road = "Road",
    Flight = "Flight"
}

